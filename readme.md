# FAN SONG: AI Audio & Video Transcriber

**FAN SONG** — это веб-утилита для автоматической транскрибации (перевода речи в текст) аудио и видеоконтента. Проект разработан для автоматизации процесса создания субтитров и текстовых расшифровок с YouTube и локальных файлов.

## 🚀 О проекте

Основная цель проекта — ускорить процесс получения текстового контента из видео. Вместо ручной расшифровки инструмент использует возможности LLM (Large Language Models) через API для мгновенного распознавания речи.

### Ключевые возможности:
- **YouTube Downloader:** Автоматическое извлечение аудиодорожки из видео по ссылке.
- **Мультиформатность:** Поддержка загрузки локальных аудио/видео файлов.
- **AI Processing:** Использование Groq API (модель Whisper) для сверхбыстрой транскрибации.
- **Web Interface:** Удобный пользовательский интерфейс для взаимодействия с системой.

---

## 🛠 Архитектура решения (Logic Flow)

Проект построен по принципу модульной архитектуры, где каждый этап обработки данных изолирован:

1.  **Input Layer:** Пользователь передает ссылку на YouTube или загружает файл.
2.  **Processing Layer:**
    * Скрипт проверяет источник данных.
    * При необходимости (YouTube) происходит скачивание аудиопотока с помощью `yt-dlp`.
    * Аудио конвертируется в совместимый формат для отправки по API.
3.  **Integration Layer:** Подготовленный файл отправляется на сервер Groq через REST API.
4.  **Output Layer:** Полученный JSON-ответ парсится, и чистый текст выводится в интерфейс пользователя с возможностью копирования.

---

## 💻 Технический стек

* **Язык:** Python 3.x
* **Интеграции:** Groq API (Whisper Model)
* **Библиотеки:**
    * `yt-dlp` (работа с YouTube)
    * `requests` / `openai` (клиент API)
    * `gradio` / `streamlit` (веб-интерфейс) *[выбери, что ты использовал, или оставь просто Web UI framework]*

---

## 👨‍💻 Об авторе и разработке

Этот проект является примером подхода **"Product-First Development"**.

* **Роль автора:** Product Owner & System Architect.
* **Задачи автора:**
    * Проектирование архитектуры приложения (Data Flow).
    * Выбор технологического стека (Groq API выбран из-за скорости инференса).
    * Написание технического задания (промпт-инжиниринг) для генерации кода.
    * Тестирование, отладка логических ошибок и сборка MVP.
* **AI-Assistance:** Кодовая база была сгенерирована с использованием LLM под управлением автора, что позволило сократить время разработки MVP (Minimum Viable Product) с нескольких дней до нескольких часов.

---

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   
   ```bash git clone [https://github.com/x9skyfall/fan-song.git]```
2. **Установите зависимости:**
    
    ```bash pip install -r requirements.txt```

 3. **Настройте переменные окружения: Создайте файл .env и добавьте ваш API ключ:**

    ```bash GROQ_API_KEY=your_api_key_here```

 4 **Установка и запуск**
 
 Важно: Для работы транскрибации необходимо, чтобы в системе был установлен FFmpeg, либо файл ffmpeg.exe должен находиться в корневой папке проекта.

5. **Запустите приложение:**
    ```bash python main.py```

---

Проект создан в образовательных целях и для демонстрации навыков проектирования систем автоматизации.
